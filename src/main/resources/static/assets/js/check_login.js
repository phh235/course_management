document.addEventListener("DOMContentLoaded", function () {    var key = 'loggedInUser';    // Lấy giá trị của key trong localStorage    var value = localStorage.getItem(key);    // Kiểm tra nếu giá trị không tồn tại    if (value === null) {        window.location.href = '/account/dang-nhap';    } else {        // Nếu giá trị tồn tại, bạn có thể làm gì đó với nó ở đây        console.log('Giá trị của key:', value);    }});